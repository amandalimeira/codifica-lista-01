// Solicita ao usuário que digite um número inteiro
let numero = Number(prompt("Digite um número inteiro:"));

// Repete o processo 10 vezes usando um loop for
for (let i = 1; i <= 10; i++) {
    // Exibe o número no console
    console.log(numero);
}